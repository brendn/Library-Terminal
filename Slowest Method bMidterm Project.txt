There are more than one methods whose complexity is O(n*n). Others have complexity less than this. One of the methods having complexity O(n*n) is saveMeTimePlease() method. There is no way to make this method more efficient.